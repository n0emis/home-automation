---
- import_playbook: dns.yml
  tags: dns

- hosts: all
  roles:
    - base
    - gantsign.oh-my-zsh
    - jnv.unattended-upgrades
  tags: base

- hosts: mail
  roles:
    - docker
    - mailcow
  tags: mailcow

- hosts: bbb
  roles:
    - n0emis.bigbluebutton
  vars:
    bbb_hostname: bbb.n0emis.eu
    bbb_turn_secret: "{{ lookup('passwordstore', 'infra/bbb.het.nbg.de.n0emis.eu/coturn') }}"
    bbb_letsencrypt_email: le@n0emis.eu
  tags: bbb
